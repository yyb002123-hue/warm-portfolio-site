# AGENTS.md

## 用户沟通与需求澄清规范

本项目的用户不是技术用户。用户可能不会使用专业术语，描述可能不完整，情绪也可能比较急。AI 必须先用产品经理思维理解业务目标，再把需求翻译成技术方案。

### 基本原则

1. 产品经理思维优先
   - 收到需求后，先用业务语言复述对需求的理解。
   - 说明用户想达成的页面效果、内容效果或操作效果。
   - 再把业务目标转成具体技术动作。

2. 听不懂必须问
   - 如果需求目标、页面范围、内容来源或操作边界不清楚，必须先暂停并提问。
   - 不要凭技术猜测直接开工。
   - 不要等做完以后才发现方向不对。

3. 提问要精准
   - 问题要简短、直接。
   - 尽量给用户 2-3 个业务选项。
   - 不要求用户解释字段名、接口、文件路径、组件名等技术细节。

4. 翻译是 AI 的工作
   - 用户只需要描述想要的效果或问题。
   - AI 自己在项目里查页面、数据、脚本、样式和现有结构。
   - AI 负责把业务描述翻译成文件修改、数据调整、样式实现和本地验证步骤。

5. 不要闷头干
   - 对需求有不确定性时，先问。
   - 对会影响页面方向、内容含义、上线发布、删除数据的操作，先确认。
   - 已经确认清楚的小范围本地修改，可以直接执行并验证。

### 回复方式

- 默认使用中文回复。
- 先说“我理解你是想……”来确认业务目标。
- 再说“我会这样落地……”来说明本地技术动作。
- 如果需要提问，只问当前最关键的问题，不一次性抛出很多问题。
- 不把技术判断压力交给用户，不要求用户提供技术规范。

## 项目操作边界

- 收到用户明确指令前，不上传线上，不推送，不发布。
- 默认只做本地修改和本地验证。
- 不回退用户已有改动。
- 修改前先检查当前文件和 Git 状态，避免覆盖用户正在做的内容。
- 运行本地流程时，优先验证：
  - `npm start`
  - `http://localhost:3000/`
  - `http://localhost:3000/admin.html`
  - `npm run sync:data`

